{
  "name": "fx-macro",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix functions && npm install --prefix client",
    "seed": "npm run seed --prefix functions",
    "dev:functions": "npm run dev --prefix functions",
    "dev:client": "npm run dev --prefix client",
    "dev": "concurrently -n functions,client -c blue,green \"npm run dev:functions\" \"npm run dev:client\"",
    "dev:local": "bash scripts/dev-local.sh",
    "build": "npm run build --prefix client",
    "deploy": "npm run build && firebase deploy --only hosting,functions"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^9.1.0"
  }
}
