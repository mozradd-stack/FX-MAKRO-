rules_version = '2';

// All reads/writes go through the Cloud Functions API (Admin SDK, which
// bypasses these rules). No client ever talks to Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
